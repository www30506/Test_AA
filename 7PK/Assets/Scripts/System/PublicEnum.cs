using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public enum SuitType{RoyalFlush, FiveKind, STRFlush, FourKind, FullHourse, Flush, Straight, ThreeKind, TwoPair, Pair, HightCard};

public enum TipType{Logo, BetOrPass, GetScore};

public enum StatusType {ReStart, OneRound, TwoRound, ThreeRound, FourRound, Win, Lose, Working};

public enum ColorType{S,H,D,C};
public class PublicEnum{}
